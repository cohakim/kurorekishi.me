#content.blog.bg-dark
  #result
    = render 'orders/result/score', order: @order
    = render 'orders/result/support', order: @order

  .primary-button.text-center(style='margin:8px')
    = link_to close_order_path, method: :patch, class: 'button radius' do
      DONE
